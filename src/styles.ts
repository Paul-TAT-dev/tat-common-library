/**
 * Opt-in stylesheet entry.
 *
 * Components in this library use Bootstrap utility classes (`btn`, `d-flex`,
 * `table-striped`, etc.) and react-bootstrap. Import this file once at the
 * root of your app to load Bootstrap's CSS and JS bundle:
 *
 *   import "@tat/common-library/styles";
 *
 * If your app already imports Bootstrap (or a compatible Bootstrap-compatible
 * theme), skip this and components will inherit your existing styles instead.
 */
import "bootstrap/dist/css/bootstrap.min.css";
import "bootstrap/dist/js/bootstrap.bundle.min.js";

export {};
